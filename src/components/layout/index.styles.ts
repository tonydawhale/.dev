import { createStyles } from '@mantine/core';

export default createStyles(() => ({
    pageContainer: {
        maxHeight: '90%',
    },
    footer: {
        position: 'fixed',
        bottom: 0,
        left: 0,
        right: 0,
    },
}));
