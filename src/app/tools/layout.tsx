import "@/styles/globals.css";

export const metadata = {
  title: 'tony\'s dev tools',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  )
}
